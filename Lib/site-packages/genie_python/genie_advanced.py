"""
Genie Advance module:

To keep the Genie Python API clean this module is intended to hold commands that are advanced features that are not
in heavy use.
"""

import sys

from genie import usercommand, helparglist, __api, _handle_exception


@usercommand
@helparglist('block object')
def get_pv_from_block(block):
    """
    Get the full PV name for a given block.

    Args:
        block (block): A block object

    Returns:
        pv_name (Str): The pv name as a string

    """
    __api.log_command(sys._getframe().f_code.co_name, locals())
    try:
        return __api.get_pv_from_block(block)
    except Exception as e:
        _handle_exception(e)
